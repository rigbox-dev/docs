---
title: Create Binding
description: Grant a role to a principal on a specific resource.
openapi: POST /api/v1/access-control/bindings
---

Resource refs use the format `rig:account:{id}`, `rig:workspace:{id}`, or `rig:app:{id}`.
