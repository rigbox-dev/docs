---
title: "rig app log-source add"
sidebarTitle: "add"
description: "Add a log source to an app"
---

Available **local and workspace**. Reference source: CLI `0.13.0-rc.2` (`09bd4024dd72`).

Use this command from your local terminal. Workspace-mode syntax, when available, is listed separately below.

## Examples

Run from your local terminal. Replace uppercase placeholders with your own values. This invocation performs the operation described above.

```bash
rig app log-source add --app APP_ID --type systemd_unit --ref SOURCE_REF
```

Inspect the supported arguments before running this command. Help is offline and does not modify resources.

```bash
rig app log-source add --help
```

## Local syntax

```text
Usage: rigbox app log-source add [OPTIONS] --app <APP> --type <SOURCE_TYPE> --ref <SOURCE_REF>
```

### Arguments and flags

| Argument | Description | Details |
| --- | --- | --- |
| `-a, --app` | App name or id | Required; Value: APP |
| `--type` | Source type: systemd_unit, docker_container, or file_path | Required; Value: SOURCE_TYPE; Choices: systemd_unit, docker_container, file_path |
| `--ref` | Unit name, container name, or absolute file path | Required; Value: SOURCE_REF |
| `-n, --name` | Display name shown in log views | Value: NAME |
| `--primary` | Mark this source as primary (default = false) | Default: false |
| `--output` | Output format [default: human, or `rig config set output …`] | Global; Value: OUTPUT; Choices: human, json, yaml, text, table |
| `--query` | Filter output with a JMESPath expression | Global; Value: JMESPATH |
| `--cli-input-json` | Read inputs from a JSON file or &#x27;-&#x27; (stdin) | Global; Value: FILE |
| `--cli-input-yaml` | Read inputs from a YAML file or &#x27;-&#x27; (stdin) | Global; Value: FILE; Conflicts: cli_input_json |
| `--generate-cli-skeleton` | Print a fillable input template, then exit | Global; Default: false |
| `--no-pager` | Do not pipe help output through a pager | Global; Default: false |
| `-v, --verbose` | Verbose diagnostics on stderr: per-phase deploy tracing, job-poll status, and transport commands. Useful for debugging a slow or stuck `rig deploy` | Global; Default: false |
| `-h, --help` | Print help |  |

<Accordion title="Complete command help">

```text
Add a log source to an app

Usage: rigbox app log-source add [OPTIONS] --app <APP> --type <SOURCE_TYPE> --ref <SOURCE_REF>

Options:
  -a, --app <APP>
          App name or id

      --output <OUTPUT>
          Output format [default: human, or `rig config set output …`]
          
          [possible values: human, json, yaml, text, table]

      --query <JMESPATH>
          Filter output with a JMESPath expression

      --type <SOURCE_TYPE>
          Source type: systemd_unit, docker_container, or file_path
          
          [possible values: systemd_unit, docker_container, file_path]

      --cli-input-json <FILE>
          Read inputs from a JSON file or '-' (stdin)

      --ref <SOURCE_REF>
          Unit name, container name, or absolute file path

      --cli-input-yaml <FILE>
          Read inputs from a YAML file or '-' (stdin)

  -n, --name <NAME>
          Display name shown in log views

      --generate-cli-skeleton
          Print a fillable input template, then exit

      --primary
          Mark this source as primary (default = false)

      --no-pager
          Do not pipe help output through a pager

  -v, --verbose
          Verbose diagnostics on stderr: per-phase deploy tracing, job-poll status, and transport commands. Useful for debugging a slow or stuck `rig deploy`

  -h, --help
          Print help
```

</Accordion>

## Workspace syntax

```text
Usage: rigbox app log-source add [OPTIONS] --app <APP> --type <SOURCE_TYPE> --ref <SOURCE_REF>
```

### Arguments and flags

| Argument | Description | Details |
| --- | --- | --- |
| `-a, --app` | App name or id | Required; Value: APP |
| `--type` | Source type: systemd_unit, docker_container, or file_path | Required; Value: SOURCE_TYPE; Choices: systemd_unit, docker_container, file_path |
| `--ref` | Unit name, container name, or absolute file path | Required; Value: SOURCE_REF |
| `-n, --name` | Display name shown in log views | Value: NAME |
| `--primary` | Mark this source as primary (default = false) | Default: false |
| `--output` | Output format | Global; Value: OUTPUT; Default: human; Choices: human, json, yaml, text, table |
| `--no-pager` | Do not pipe help output through a pager | Global; Default: false |
| `-v, --verbose` | Verbose diagnostics on stderr (per-phase deploy tracing, job-poll status) | Global; Default: false |
| `-h, --help` | Print help |  |

<Accordion title="Complete command help">

```text
Add a log source to an app

Usage: rigbox app log-source add [OPTIONS] --app <APP> --type <SOURCE_TYPE> --ref <SOURCE_REF>

Options:
  -a, --app <APP>
          App name or id

      --output <OUTPUT>
          Output format
          
          [default: human]
          [possible values: human, json, yaml, text, table]

      --no-pager
          Do not pipe help output through a pager

      --type <SOURCE_TYPE>
          Source type: systemd_unit, docker_container, or file_path
          
          [possible values: systemd_unit, docker_container, file_path]

      --ref <SOURCE_REF>
          Unit name, container name, or absolute file path

  -v, --verbose
          Verbose diagnostics on stderr (per-phase deploy tracing, job-poll status)

  -n, --name <NAME>
          Display name shown in log views

      --primary
          Mark this source as primary (default = false)

  -h, --help
          Print help
```

</Accordion>
